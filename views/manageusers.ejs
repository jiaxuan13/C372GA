<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FluffyFriend — Manage Users</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    html, body { height: 100%; margin: 0; display: flex; flex-direction: column; }
    main { flex: 1; }
    footer { background: linear-gradient(90deg, #ff8db8, #ff6fa3); color: white; padding: 12px 0; margin-top: auto; }
    :root{ --pet-700:#c94b7b; --pet-600:#ff6fa3; --pet-100:#ffe9f2; }
    .badge-role{ background: var(--pet-100); color:#7e2a47; border:1px solid var(--pet-600); }
    .btn-pink{ background-color: var(--pet-600); border-color: var(--pet-600); color:#fff; }
    .btn-pink:hover{ filter: brightness(0.95); }
  </style>
</head>
<body class="bg-light">
  <main>
  <div class="container py-4">
    <h1 class="h4 mb-3">Manage Users</h1>
    <p class="text-muted">Admin-only. Update roles exactly like the sample flow.</p>

    <% if (typeof success !== 'undefined' && success && success.length){ %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>

    <div class="card shadow-sm">
      <div class="card-body table-responsive">
        <table class="table align-middle">
          <thead>
            <tr>
              <th>#</th>
              <th>Username</th>
              <th>Email</th>
              <th>Role</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% users.forEach(function(u){ %>
              <tr>
                <td><%= u.users_id %></td>
                <td><%= u.username %></td>
                <td><%= u.email %></td>
                <td><span class="badge badge-role"><%= u.role %></span></td>
                <td>
                  <!-- SAME route & method names as sample app.js. -->
                  <form method="post" action="/updateRole/<%= u.users_id %>" class="d-flex gap-2">
                    <select name="role" class="form-select form-select-sm" required>
                      <option value="customer" <%= u.role==='customer'?'selected':'' %>>customer</option>
                      <option value="admin" <%= u.role==='admin'?'selected':'' %>>admin</option>
                    </select>
                    <button class="btn btn-pink btn-sm" type="submit">Update</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  </main>
  <footer>
    <div class="container d-flex justify-content-between align-items-center">
      <small>© <%= new Date().getFullYear() %> FluffyFriend.</small>
      <small>Made with ♥ in a pink theme.</small>
    </div>
  </footer>
</body>
</html>
