<!-- Isaac Start -->
<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Admin — Products | FluffyFriend</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      display: flex;
      flex-direction: column;
      font-family: 'Segoe UI', sans-serif;
      background:#fff6fb;
    }
    main { flex: 1; }

    .navbar { background:linear-gradient(90deg,#ff8db8,#ff6fa3); }
    .navbar-brand { color:#fff; font-weight:700; }
    .nav-link { color:#fff !important; font-weight:500; border-radius:8px; padding:8px 14px; }

    footer { background: linear-gradient(90deg, #ff8db8, #ff6fa3); color: white; padding: 12px 0; margin-top: auto; }

    .product-thumb { width:60px; height:60px; border-radius:10px; object-fit:cover; }

    /* Remove excess row padding */
    .table td, 
    .table th {
      padding: 6px 10px !important; /* compact & clean */
    }

    .row-link { cursor: pointer; }
    .row-link:hover { background: #fff1f7 !important; }
  </style>
</head>
<body>

  <!-- Admin navbar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container d-flex justify-content-between">
      <a class="navbar-brand" href="/">🐾 FluffyFriend</a>
      <div>
        <a class="nav-link d-inline" href="/admin">Admin</a>
        <a class="nav-link d-inline" href="/products">View Store</a>
        <a class="nav-link d-inline" href="/logout">Logout</a>
      </div>
    </div>
  </nav>

  <main>
    <div class="container mt-3">
      <% if (success && success.length) { %>
        <div class="alert alert-success"><%= success[0] %></div>
      <% } %>
      <% if (error && error.length) { %>
        <div class="alert alert-danger"><%= error[0] %></div>
      <% } %>
    </div>

    <div class="container my-4">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h3 class="mb-0">Manage Products</h3>
        <a href="/admin/products/new" class="btn btn-sm btn-primary">+ Add Product</a>
      </div>

      <div class="table-responsive">
        <table class="table table-striped align-middle">
          <thead>
            <tr>
              <th>ID</th>
              <th>Image</th>
              <th>Name</th>
              <th>Category</th>
              <th class="text-end">Price</th>
              <th class="text-end">Qty</th>
              <th class="text-end">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (products && products.length) { %>
              <% products.forEach(function (p) { %>
                <tr class="row-link" data-href="/viewProduct/<%= p.id %>">

                  <td><%= p.id %></td>

                  <td>
                    <% if (p.image) { %>
                      <img src="/images/products/<%= p.image %>"
                           alt="<%= p.productName %>"
                           class="product-thumb">
                    <% } else { %>
                      <span class="text-muted small">No image</span>
                    <% } %>
                  </td>

                  <td>
                    <a href="/viewProduct/<%= p.id %>" class="text-dark fw-bold text-decoration-none no-row-nav">
                      <%= p.productName %>
                    </a>
                  </td>

                  <td><%= p.category || '-' %></td>
                  <td class="text-end">$<%= Number(p.price).toFixed(2) %></td>
                  <td class="text-end"><%= p.quantity %></td>

                  <td class="text-end">
                    <a href="/admin/products/<%= p.id %>/edit"
                       class="btn btn-sm btn-outline-secondary no-row-nav">Edit</a>
                  </td>

                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="7" class="text-center text-muted py-4">
                  No products yet. Click “Add Product” to create one.
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>

  <script>
    document.addEventListener('DOMContentLoaded', function () {
      document.querySelectorAll('tr.row-link').forEach(function (row) {
        row.addEventListener('click', function (e) {
          if (e.target.closest('.no-row-nav') || e.target.tagName === 'A' || e.target.tagName === 'BUTTON') return;
          window.location.href = row.dataset.href;
        });
      });
    });
  </script>

</body>
</html>
<!-- Isaac End -->