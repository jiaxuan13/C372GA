<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Edit Product | FluffyFriend Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      display: flex;
      flex-direction: column;
      font-family:'Segoe UI', sans-serif;
      background:#fff6fb;
    }
    main { flex: 1; }
    footer {
      background: linear-gradient(90deg, #ff8db8, #ff6fa3);
      color: white;
      padding: 12px 0;
      margin-top: auto;
    }
    .navbar {
      background:linear-gradient(90deg,#ff8db8,#ff6fa3);
    }
    .navbar-brand { color:#fff; font-weight:700; }
    .nav-link {
      color:#fff !important;
      font-weight:500;
      border-radius:8px;
      padding:8px 14px;
    }
    .card-soft {
      background:#fff;
      border-radius:16px;
      box-shadow:0 3px 12px rgba(0,0,0,.08);
      border:0;
      padding:22px 24px;
      max-width:700px;
      margin:30px auto 40px;
    }
    .btn-pink { background:#ff6fa3; border:none; color:#fff; }
    .btn-pink:hover { background:#ff8db8; color:#fff; }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container d-flex justify-content-between">
      <a class="navbar-brand" href="/">🐾 FluffyFriend</a>
      <div>
        <a class="nav-link d-inline" href="/admin">Admin</a>
        <a class="nav-link d-inline" href="/admin/products">Products</a>
        <a class="nav-link d-inline" href="/logout">Logout</a>
      </div>
    </div>
  </nav>

  <main>
    <!-- Flash messages -->
    <div class="container mt-3">
      <% if (success && success.length) { %>
        <div class="alert alert-success"><%= success[0] %></div>
      <% } %>
      <% if (error && error.length) { %>
        <div class="alert alert-danger"><%= error[0] %></div>
      <% } %>
    </div>

    <div class="card-soft">
      <h4 class="fw-bold mb-3">Edit Product</h4>

      <form action="/admin/products/<%= product.id %>/edit"
            method="POST"
            enctype="multipart/form-data">

        <!-- Name -->
        <div class="mb-3">
          <label class="form-label">Product Name</label>
          <input
            type="text"
            name="productName"
            class="form-control"
            required
            value="<%= product.productName %>">
        </div>

        <!-- Description -->
        <div class="mb-3">
          <label class="form-label">Description</label>
          <textarea
            name="description"
            class="form-control"
            rows="4"
            required><%= product.description || '' %></textarea>
        </div>

        <!-- Category -->
        <div class="mb-3">
          <label class="form-label">Category</label>
          <input
            type="text"
            name="category"
            class="form-control"
            required
            value="<%= product.category || '' %>">
        </div>

        <!-- Quantity + Price -->
        <div class="row">
          <div class="mb-3 col-md-6">
            <label class="form-label">Quantity</label>
            <input
              type="number"
              min="0"
              name="quantity"
              class="form-control"
              required
              value="<%= product.quantity %>">
          </div>

          <div class="mb-3 col-md-6">
            <label class="form-label">Price (SGD)</label>
            <input
              type="number"
              step="0.01"
              min="0"
              name="price"
              class="form-control"
              required
              value="<%= product.price %>">
          </div>
        </div>

        <!-- Current image -->
        <div class="mb-3">
          <label class="form-label">Current Image</label><br>
          <% if (product.image) { %>
            <img
              src="/images/products/<%= product.image %>"
              alt="<%= product.productName %>"
              style="width:120px;height:120px;border-radius:10px;object-fit:cover;">
          <% } else { %>
            <span class="text-muted small">No image uploaded yet.</span>
          <% } %>
        </div>

        <!-- New image -->
        <div class="mb-3">
          <label class="form-label">Upload New Image (optional)</label>
          <input type="file" name="image" class="form-control">
          <!-- keep old file if no new upload -->
          <input type="hidden" name="currentImage" value="<%= product.image || '' %>">
        </div>

        <div class="d-flex justify-content-between mt-3">
          <a href="/admin/products" class="btn btn-outline-secondary">Back</a>
          <button type="submit" class="btn btn-pink">Save Changes</button>
        </div>
      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
