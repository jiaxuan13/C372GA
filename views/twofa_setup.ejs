<!Doctype html>
<html>
<head><meta charset="utf-8"><title>Enable 2FA</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-4" style="max-width:640px;">
  <h2 class="mb-3">Enable Two-Factor Authentication (TOTP)</h2>
  <p class="text-muted">Scan this QR with Google Authenticator / Microsoft Authenticator, then enter the 6-digit code.</p>
  <img src="<%= qrcodeDataUrl %>" alt="QR Code" class="border rounded mb-3" style="max-width:240px;">
  <form method="POST" action="/2fa/setup" class="d-flex gap-2">
    <input name="token" class="form-control" placeholder="6-digit code" required maxlength="6" pattern="\\d{6}">
    <button class="btn btn-primary" style="background:#ff6fa3;border:0;">Confirm & Enable</button>
  </form>
</body>
</html>

