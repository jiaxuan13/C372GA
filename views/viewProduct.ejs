<!-- Isaac Start -->
<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= product.productName %> | FluffyFriend</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      display: flex;
      flex-direction: column;
      font-family: 'Segoe UI', sans-serif;
      background:#fff6fb;
    }
    main { flex: 1; }
    .navbar { background:linear-gradient(90deg,#ff8db8,#ff6fa3); }
    .navbar-brand { color:#fff; font-weight:700; }
    .nav-link { color:#fff !important; font-weight:500; border-radius:8px; padding:8px 14px; }
    .price { font-weight:700; color:#ff4c93; }
    footer { background: linear-gradient(90deg, #ff8db8, #ff6fa3); color: white; padding: 12px 0; margin-top: auto; }
  </style>
</head>

<body>
  <!-- Admin navbar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container d-flex justify-content-between">
      <a class="navbar-brand" href="/">🐾 FluffyFriend</a>
      <div>
        <a class="nav-link d-inline" href="/admin">Admin</a>
        <a class="nav-link d-inline" href="/products">View Store</a>
        <a class="nav-link d-inline" href="/logout">Logout</a>
      </div>
    </div>
  </nav>

  <main>
    <div class="container py-5">
      <a href="/admin/products" class="btn btn-secondary mb-3">&larr; Back to Products</a>

      <div class="row">
        <div class="col-md-5">
          <% if (product.image) { %>
            <img src="/images/products/<%= product.image %>"
                 class="img-fluid rounded mb-3"
                 alt="<%= product.productName %>">
          <% } else { %>
            <div class="border rounded d-flex align-items-center justify-content-center mb-3" style="height:300px;background:#fff;">
              <span class="text-muted">No image</span>
            </div>
          <% } %>
        </div>

        <div class="col-md-7">
          <h2 class="mb-2"><%= product.productName %></h2>
          <p class="text-muted mb-2">Category: <%= product.category || '-' %></p>
          <p class="price fs-4 mb-3">$<%= Number(product.price).toFixed(2) %></p>
          <p class="mb-2">Stock available: <strong><%= product.quantity %></strong></p>

          <% 
            const desc = (product.description && String(product.description).trim().length)
              ? product.description
              : 'No description available.';
          %>
          <div class="mt-3">
            <h5 class="mb-2">Description</h5>
            <p class="mb-0"><%= desc %></p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
<!-- Isaac End -->
